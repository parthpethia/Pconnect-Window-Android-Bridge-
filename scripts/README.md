# Scripts

- `build-desktop.ps1` — publishes the Windows agent as a single-file EXE (requires .NET SDK)
- `bootstrap-mobile.ps1` — generates missing Flutter platform folders and runs `flutter pub get`
